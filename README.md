# Lab-Creation-on-MERN-Stack

## In order to populate the "Shop" database with Products and Users Collection, use the following command(inside /home/labDirectory):

`mongosh localhost:27017/Shop populate.js`
